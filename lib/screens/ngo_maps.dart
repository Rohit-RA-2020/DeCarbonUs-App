import 'package:flutter/material.dart';

class NgoMaps extends StatelessWidget {
  const NgoMaps({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('NGO Maps'),
      ),
    );
  }
}
